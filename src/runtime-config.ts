// src/runtime-config.ts
// Runtime configuration for the component documentation system

import { ComponentDocOptions } from './types';

// Export an empty configuration object or customize as needed
export const runtimeConfig: Partial<ComponentDocOptions> = {
  // You can add specific runtime configuration options here
  // For example:
  // enableDocs: true,
  // mainAppID: 'app',
  // Add other options as needed based on your requirements
};